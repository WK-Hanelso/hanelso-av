"""SparseDriveV2 — System 1 온보딩 슬롯 (1단계: 자리잡기).

registry 미구현 상태. 온보딩 단계·수정 계획·체크리스트는 이 디렉토리의
README.md(명세 SoT)를 따른다. 조립 시도 시 아래 에러로 fail-fast한다.
"""

raise ImportError(
    "planning model 'sparsedrive_v2' is not implemented yet (onboarding stage 1: "
    "slot/spec only). See planning/models/sparsedrive_v2/README.md for the "
    "onboarding plan. Use --model pluto for the current pipeline."
)
